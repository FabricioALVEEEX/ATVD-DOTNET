@model MacLancheFelizFabricio.Models.Produto
@{
    ViewData["Title"] = "Excluir Produto";
}

<h2 class="mt-4 text-danger">🗑️ Confirmar Exclusão</h2>

<h4>Tem certeza que deseja excluir o produto abaixo?</h4>
<div>
    <dl class="row">
        <dt class="col-sm-2">Nome:</dt>
        <dd class="col-sm-10">@Model.Nome</dd>

        <dt class="col-sm-2">Preço:</dt>
        <dd class="col-sm-10">@Model.Preco.ToString("C")</dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <button type="submit" class="btn btn-danger">Excluir</button>
        <a asp-action="Index" class="btn btn-secondary ms-2">Cancelar</a>
    </form>
</div>
